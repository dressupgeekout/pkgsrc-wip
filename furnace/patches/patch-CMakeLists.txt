$NetBSD$

NetBSD does not have libdl

--- CMakeLists.txt.orig	2023-07-11 22:59:24.904337330 -0700
+++ CMakeLists.txt	2023-07-11 23:01:17.644098064 -0700
@@ -830,7 +830,9 @@
   find_library(COCOA Cocoa REQUIRED)
   list(APPEND DEPENDENCIES_LIBRARIES ${COCOA})
 else()
+  if (NOT ${CMAKE_SYSTEN_NAME} STREQUAL "NetBSD")
   list(APPEND DEPENDENCIES_LIBRARIES dl)
+  endif()
 endif()
 
 if (NOT MSVC)
