$NetBSD$

XXX

--- CMakeLists.txt.orig	2023-08-22 23:47:48.979967538 -0700
+++ CMakeLists.txt	2023-08-22 23:51:36.339270636 -0700
@@ -61,6 +61,9 @@
 	add_library(${NCINE_APP} STATIC)
 else()
 	add_executable(${NCINE_APP})
+	if(${CMAKE_SYSTEM_NAME} STREQUAL "NetBSD")
+		target_compile_definitions(${NCINE_APP} PUBLIC _NETBSD_SOURCE)
+	endif()
 	if(WINDOWS_PHONE OR WINDOWS_STORE)
 		message(STATUS "Compiling for Windows RT")
 	else()
